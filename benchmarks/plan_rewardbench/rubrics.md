# Plan-RewardBench Rubrics

The exact local copy of the Plan-RewardBench Appendix C rubric prompts lives in:

`benchmarks/plan_rewardbench/rubric_prompts.py`

Use `APPENDIX_C_RUBRICS` from that module as the canonical source. This markdown
file intentionally does not duplicate the rubric text, so the harness and
proposer guidance cannot drift from a second compressed copy.
